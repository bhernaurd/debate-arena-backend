diff --git a/lib/accountRankedDebateService.js b/lib/accountRankedDebateService.js
index 82948bf..2ebeb1f 100644
--- a/lib/accountRankedDebateService.js
+++ b/lib/accountRankedDebateService.js
@@ -1,4 +1,5 @@
 import crypto from 'node:crypto';
+import { normalizeLanguageCode } from './languageSupport.js';
 
 import {
     requireRankedPhilosopher,
@@ -1968,6 +1969,9 @@ function normalizeDebate(
         philosopherId,
         philosopherName,
         debateMode,
+        languageCode: normalizeLanguageCode(
+            rowValue(row, 'language_code', 'languageCode')
+        ),
         topic:
             normalizeText(
                 rowValue(
@@ -1978,6 +1982,13 @@ function normalizeDebate(
                 'debate.topic',
                 4_000
             ),
+        displayTopic:
+            normalizeText(
+                rowValue(row, 'display_topic', 'displayTopic') ??
+                    rowValue(row, 'topic', 'topic'),
+                'debate.displayTopic',
+                4_000
+            ),
         topicFingerprint,
         topicTheme:
             normalizeText(
@@ -2357,7 +2368,8 @@ function publicDebate(debate) {
             debate.philosopherName,
         debateMode:
             debate.debateMode,
-        topic: debate.topic,
+        topic: debate.displayTopic,
+        language: debate.languageCode,
         topicFingerprint:
             debate.topicFingerprint,
         topicTheme:
@@ -4139,6 +4151,7 @@ function generationContext(debate) {
             debate.philosopher,
         debateMode:
             debate.debateMode,
+        language: debate.languageCode,
         topic: debate.topic,
         topicTheme:
             debate.topicTheme,
@@ -4187,7 +4200,9 @@ export function createPostgresAccountRankedDebateRepository(
         philosopher_id,
         philosopher_name,
         debate_mode,
+        language_code,
         topic,
+        display_topic,
         topic_fingerprint,
         topic_theme,
         topic_model_provider,
