diff --git a/lib/accountRankedLadderService.js b/lib/accountRankedLadderService.js
index 9991795..dde0fd0 100644
--- a/lib/accountRankedLadderService.js
+++ b/lib/accountRankedLadderService.js
@@ -1,4 +1,5 @@
 import { randomUUID } from 'node:crypto';
+import { normalizeLanguageCode } from './languageSupport.js';
 
 import {
     isRankedPhilosopherID,
@@ -580,7 +581,13 @@ function normalizeDebate(row, expectedAccountId) {
             100
         ),
         debateMode: requireMode(rowValue(row, 'debate_mode', 'debateMode')),
+        languageCode: normalizeLanguageCode(rowValue(row, 'language_code', 'languageCode')),
         topic: normalizeText(rowValue(row, 'topic', 'topic'), 'debate.topic', 4_000),
+        displayTopic: normalizeText(
+            rowValue(row, 'display_topic', 'displayTopic') ?? rowValue(row, 'topic', 'topic'),
+            'debate.displayTopic',
+            4_000
+        ),
         topicFingerprint: requireString(
             rowValue(row, 'topic_fingerprint', 'topicFingerprint'),
             'debate.topicFingerprint',
@@ -724,7 +731,7 @@ function requireGeneratedString(
     return cleaned;
 }
 
-function normalizeGeneratedTopic(result, expectedPhilosopher, expectedMode) {
+function normalizeGeneratedTopic(result, expectedPhilosopher, expectedMode, expectedLanguage) {
     if (!result || typeof result !== 'object' || Array.isArray(result)) {
         fail(
             'ranked_topic_generation_failed',
@@ -736,7 +743,8 @@ function normalizeGeneratedTopic(result, expectedPhilosopher, expectedMode) {
     if (
         result.philosopherId !== expectedPhilosopher.id ||
         result.philosopherName !== expectedPhilosopher.name ||
-        result.debateMode !== expectedMode
+        result.debateMode !== expectedMode ||
+        (result.language != null && normalizeLanguageCode(result.language) !== expectedLanguage)
     ) {
         fail(
             'ranked_topic_generation_mismatch',
@@ -753,6 +761,10 @@ function normalizeGeneratedTopic(result, expectedPhilosopher, expectedMode) {
             minimumLength: 30,
             maximumLength: 220,
         }),
+        displayTopic: requireGeneratedString(result.displayTopic, 'generatedTopic.displayTopic', {
+            minimumLength: 12,
+            maximumLength: 280,
+        }),
         openingQuestion: requireGeneratedString(
             result.openingQuestion,
             'generatedTopic.openingQuestion',
@@ -783,6 +795,7 @@ function normalizeGeneratedTopic(result, expectedPhilosopher, expectedMode) {
             'generatedTopic.generatorVersion',
             { maximumLength: 100 }
         ),
+        language: expectedLanguage,
     });
 }
 
@@ -832,17 +845,19 @@ function createSeededOpeningMessage({
     });
 }
 
-function validateRequestIdentity(row, requestId, philosopher, debateMode) {
+function validateRequestIdentity(row, requestId, philosopher, debateMode, languageCode) {
     const storedRequestId = String(rowValue(row, 'request_id', 'requestId') ?? '').toLowerCase();
     const storedKind = rowValue(row, 'debate_kind', 'debateKind');
     const storedPhilosopherId = rowValue(row, 'philosopher_id', 'philosopherId');
     const storedMode = rowValue(row, 'debate_mode', 'debateMode');
+    const storedLanguage = normalizeLanguageCode(rowValue(row, 'language_code', 'languageCode'));
 
     if (
         storedRequestId !== requestId ||
         storedKind !== 'ladder' ||
         storedPhilosopherId !== philosopher.id ||
-        storedMode !== debateMode
+        storedMode !== debateMode ||
+        storedLanguage !== languageCode
     ) {
         fail(
             'ranked_start_request_reused',
@@ -941,7 +956,9 @@ export function createPostgresAccountRankedLadderRepository(pool) {
         philosopher_id,
         philosopher_name,
         debate_mode,
+        language_code,
         topic,
+        display_topic,
         topic_fingerprint,
         topic_theme,
         topic_model_provider,
@@ -1126,6 +1143,7 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                 requestId,
                 philosopher,
                 debateMode,
+                languageCode,
                 createdAt,
             }
         ) {
@@ -1140,6 +1158,7 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         philosopher_id,
                         philosopher_name,
                         debate_mode,
+                        language_code,
                         status,
                         created_at,
                         updated_at
@@ -1152,9 +1171,10 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         $3::text,
                         $4::text,
                         $5::text,
+                        $6::text,
                         'reserved',
-                        $6::timestamptz,
-                        $6::timestamptz
+                        $7::timestamptz,
+                        $7::timestamptz
                     )
                     RETURNING *
                 `,
@@ -1164,6 +1184,7 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                     philosopher.id,
                     philosopher.name,
                     debateMode,
+                    languageCode,
                     createdAt,
                 ]
             );
@@ -1179,6 +1200,7 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                     SET
                         status = 'reserved',
                         topic = NULL,
+                        display_topic = NULL,
                         topic_normalized = NULL,
                         topic_fingerprint = NULL,
                         topic_theme = NULL,
@@ -1262,8 +1284,9 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         topic_model_name = $8::text,
                         topic_generator_version = $9::text,
                         opening_question = $10::text,
-                        topic_generated_at = $11::timestamptz,
-                        updated_at = $11::timestamptz
+                        display_topic = $11::text,
+                        topic_generated_at = $12::timestamptz,
+                        updated_at = $12::timestamptz
                     WHERE account_id = $1::uuid
                       AND request_id = $2::uuid
                       AND status = 'reserved'
@@ -1280,6 +1303,7 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                     generatedTopic.model,
                     generatedTopic.generatorVersion,
                     generatedTopic.openingQuestion,
+                    generatedTopic.displayTopic,
                     generatedAt,
                 ]
             );
@@ -1324,7 +1348,9 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         philosopher_id,
                         philosopher_name,
                         debate_mode,
+                        language_code,
                         topic,
+                        display_topic,
                         topic_source,
                         topic_fingerprint,
                         topic_theme,
@@ -1362,18 +1388,18 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         $4::text,
                         $5::text,
                         $6::text,
-                        'system_generated',
                         $7::text,
                         $8::text,
+                        'system_generated',
                         $9::text,
                         $10::text,
-                        $11::timestamptz,
+                        $11::text,
                         $12::text,
-                        $13::smallint,
-                        $14::integer,
-                        $15::integer,
-                        $16::text,
-                        $17::text,
+                        $13::timestamptz,
+                        $14::text,
+                        $15::smallint,
+                        $16::integer,
+                        $17::integer,
                         $18::text,
                         $19::text,
                         $20::text,
@@ -1381,13 +1407,15 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                         $22::text,
                         $23::text,
                         $24::text,
-                        $24::text,
-                        $25::timestamptz,
-                        $25::timestamptz,
-                        $25::timestamptz,
-                        $25::timestamptz,
+                        $25::text,
+                        $26::text,
                         $26::text,
-                        $27::jsonb
+                        $27::timestamptz,
+                        $27::timestamptz,
+                        $27::timestamptz,
+                        $27::timestamptz,
+                        $28::text,
+                        $29::jsonb
                     )
                     RETURNING ${ladderDebateColumns}
                 `,
@@ -1397,7 +1425,9 @@ export function createPostgresAccountRankedLadderRepository(pool) {
                     philosopher.id,
                     philosopher.name,
                     debateMode,
+                    generatedTopic.language,
                     generatedTopic.topic,
+                    generatedTopic.displayTopic,
                     generatedTopic.topicFingerprint,
                     generatedTopic.theme,
                     TOPIC_MODEL_PROVIDER,
@@ -1598,7 +1628,7 @@ export function createAccountRankedLadderService({
         } catch {}
     }
 
-    async function reserve({ accountId, requestId, philosopher, debateMode }) {
+    async function reserve({ accountId, requestId, philosopher, debateMode, languageCode }) {
         const checkedAt = serviceDate(now);
         const staleBefore = new Date(checkedAt.getTime() - startLeaseMs);
 
@@ -1617,7 +1647,7 @@ export function createAccountRankedLadderService({
             });
 
             if (request) {
-                validateRequestIdentity(request, requestId, philosopher, debateMode);
+                validateRequestIdentity(request, requestId, philosopher, debateMode, languageCode);
 
                 if (rowValue(request, 'status', 'status') === 'completed') {
                     const existingDebate = normalizeDebate(
@@ -1737,6 +1767,7 @@ export function createAccountRankedLadderService({
                     requestId,
                     philosopher,
                     debateMode,
+                    languageCode,
                     createdAt: checkedAt,
                 });
 
@@ -1795,7 +1826,7 @@ export function createAccountRankedLadderService({
                 );
             }
 
-            validateRequestIdentity(request, requestId, philosopher, debateMode);
+            validateRequestIdentity(request, requestId, philosopher, debateMode, generatedTopic.language);
 
             if (rowValue(request, 'status', 'status') === 'completed') {
                 return Object.freeze({
@@ -1935,10 +1966,12 @@ export function createAccountRankedLadderService({
         requestId,
         philosopherId,
         debateMode,
+        language = 'en',
     }) {
         const cleanRequestId = requireRequestId(requestId);
         const philosopher = requireCanonicalPhilosopher(philosopherId);
         const cleanDebateMode = requireMode(debateMode);
+        const languageCode = normalizeLanguageCode(language);
         const authorization = await authorize({ installationId, accessToken });
 
         await requirePro(authorization.accountId);
@@ -1951,6 +1984,7 @@ export function createAccountRankedLadderService({
                 requestId: cleanRequestId,
                 philosopher,
                 debateMode: cleanDebateMode,
+                languageCode,
             });
         } catch (error) {
             const conflict = postgresConflict(error);
@@ -1984,12 +2018,14 @@ export function createAccountRankedLadderService({
                 philosopherId: philosopher.id,
                 debateMode: cleanDebateMode,
                 recentTopics: reservation.recentTopics,
+                ...(languageCode === 'en' ? {} : { language: languageCode }),
             });
 
             generatedTopic = normalizeGeneratedTopic(
                 rawTopic,
                 philosopher,
-                cleanDebateMode
+                cleanDebateMode,
+                languageCode
             );
         } catch (error) {
             const publicError = mapDependencyError(error, {
