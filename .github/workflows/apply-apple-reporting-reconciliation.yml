name: Apply Apple reporting reconciliation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-apple-reporting-reconciliation.yml'

permissions:
  contents: write

# Retry after tightening the regression assertion.
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply patch
        run: node scripts/patchAppleReportingReconciliation.mjs
      - name: Install dependencies
        run: npm ci
      - name: Run syntax checks
        run: npm run check
      - name: Run automated tests
        run: npm test
      - name: Remove temporary patch files
        run: |
          rm scripts/patchAppleReportingReconciliation.mjs
          rm .github/workflows/apply-apple-reporting-reconciliation.yml
      - name: Commit final changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Use reconciliation for Apple reporting status"
          git push
